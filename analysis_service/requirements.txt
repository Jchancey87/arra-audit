# Self-contained install: pip install -r requirements.txt
# The extra-index-url lets pip resolve the CUDA 12.6 torch wheel (local +cu126
# tag) without a separate manual step. PyPI is still used for every other pkg.
--extra-index-url https://download.pytorch.org/whl/cu126

fastapi>=0.100.0
uvicorn>=0.20.0
pydantic>=2.0
requests>=2.28.0
yt-dlp>=2023.3.3

# ML Analysis / GPU Support — GTX 1050 Ti (sm_61, CUDA 12.6)
# torch 2.6.0+cu126 is the last 2.x line that ships cuDNN 9.x wheels with
# sm_61 in its cap list... actually sm_61 is NOT in the cap list, which is
# why analyzer.py disables cuDNN at runtime on (6,1) devices. Do NOT bump
# to a cu13 build — that pulls nvidia-*-cu13 packages which conflict with
# the cu126 runtime the rest of the stack expects.
torch==2.6.0+cu126
transformers>=4.30.0
accelerate>=0.20.0
librosa>=0.10.0
soundfile>=0.12.0

# CLAP audio model — `transformers.ClapModel` (no laion-clap package).
# Default checkpoint is `laion/larger_clap_music` (~1.2GB, music-tuned, better
# zero-shot accuracy). On sm_61 (Pascal) the analyzer disables cuDNN and
# runs in FP32 (see analyzer.py sm_61 workaround block). Set
# `CLAP_MODEL=laion/clap-htsat-fused` in ../.env to fall back to the smaller
# 600MB model (cuDNN-friendly, FP16).
